services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: flask run --host=0.0.0.0 --port=5000 --debug
    extra_hosts:
      - "host.docker.internal:host-gateway"